name: flutter_device_ring
description: A customizable traffic utilization ring gauge widget for Flutter. Displays network utilization with animated arcs, tier colors, and smooth state transitions.
version: 0.3.0
homepage: https://github.com/lianghualin/flutter_device_ring
repository: https://github.com/lianghualin/flutter_device_ring
issue_tracker: https://github.com/lianghualin/flutter_device_ring/issues
topics:
  - widget
  - gauge
  - network
  - animation

environment:
  sdk: ^3.5.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
